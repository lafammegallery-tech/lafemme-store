# La Femme V1.5.0 — Admin, Payment & Security

## انجام‌شده
- پنل عملیاتی مدیریت محصولات، سفارش‌ها، موجودی، کاربران و تاریخچه قیمت بازار
- تغییر وضعیت سفارش همراه با تاریخچه و Audit Log
- بازگردانی رزرو موجودی هنگام لغو سفارش
- زیرساخت Payment Provider و درگاه Mock قابل تست
- تأیید idempotent پرداخت، ثبت تراکنش، تأیید سفارش و اعلان کاربر
- مدل و Migration تاریخچه وضعیت سفارش و کلید idempotency پرداخت
- مدیریت واقعی آدرس‌های کاربر
- هزینه ارسال و Snapshot آدرس در Checkout
- اعتبارسنجی و پاک‌سازی پایه ورودی‌ها
- Rate limiter حافظه‌ای برای استفاده در Routeهای حساس

## محدودیت‌های آگاهانه
- درگاه واقعی بانکی به Merchant/API Key نیاز دارد؛ این نسخه Mock Provider دارد.
- Rate limiter حافظه‌ای برای یک نمونه سرور مناسب است؛ در Production چندنمونه‌ای باید Redis استفاده شود.
- آپلود واقعی تصویر محصول و CRUD کامل دسته‌بندی هنوز در فاز نهایی است.
- پیامک و ایمیل واقعی نیازمند سرویس‌دهنده هستند.

## نتیجه بررسی فنی این محیط
- `npm install`: موفق
- `npm run typecheck`: موفق
- `npm run build`: کامپایل و Type/Lint موفق؛ در مرحله تولید صفحات استاتیک به محدودیت زمانی محیط تحویل رسید.
- `prisma generate`: به دلیل خطای موقت DNS در دریافت engine از `binaries.prisma.sh` در این محیط تکمیل نشد. فایل schema و migration آماده‌اند و روی سیستم دارای اینترنت با `npx prisma generate` اجرا می‌شوند.
