/* eslint-disable @typescript-eslint/no-explicit-any */
import { getPrisma } from "@/backend/database/prisma";
export async function evaluateCoupon(code:string, subtotal:number, userId?:string) { if(!code) return null; const p:any=getPrisma(); const coupon=await p.coupon.findUnique({where:{code:code.trim().toUpperCase()},include:{_count:{select:{usages:true}}}}); const now=new Date(); if(!coupon||!coupon.isActive||(coupon.startsAt&&coupon.startsAt>now)||(coupon.expiresAt&&coupon.expiresAt<now)||Number(coupon.minimumAmount)>subtotal) return null; if(coupon.usageLimit&&coupon._count.usages>=coupon.usageLimit) return null; if(userId){const used=await p.couponUsage.count({where:{couponId:coupon.id,userId}});if(used>=coupon.perUserLimit)return null;} let discount=0,freeShipping=false;if(coupon.type==="PERCENT") discount=Math.floor(subtotal*Number(coupon.value)/100);if(coupon.type==="FIXED")discount=Number(coupon.value);if(coupon.type==="FREE_SHIPPING")freeShipping=true;if(coupon.maximumDiscount)discount=Math.min(discount,Number(coupon.maximumDiscount));discount=Math.min(discount,subtotal);return {coupon,discount,freeShipping};}
