import Link from "next/link";import { startPaymentAction } from "@/app/actions/payment";import { PageLayout,PageHero } from "@/components/common";import { Container,Card,Button } from "@/components/ui";export default async function Page({searchParams}:{searchParams:Promise<{order?:string}>}){const {order}=await searchParams;return <PageLayout><PageHero title="سفارش ثبت شد" description="درخواست خرید شما با موفقیت ثبت شد."/><section className="py-16"><Container><Card className="p-8 text-center"><p>شماره سفارش: <strong>{order||"-"}</strong></p><p className="mt-3">برای آزمایش چرخه کامل سفارش، پرداخت شبیه‌سازی‌شده را اجرا کنید.</p>{order&&<form action={startPaymentAction} className="mt-6"><input type="hidden" name="orderNumber" value={order}/><Button type="submit">پرداخت آزمایشی موفق</Button></form>}<Link className="btn btn-secondary mt-4 inline-block" href="/orders">مشاهده سفارش‌ها</Link></Card></Container></section></PageLayout>}
