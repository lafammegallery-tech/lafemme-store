import Link from "next/link";import { PageLayout,PageHero } from "@/components/common";import { Container,Card } from "@/components/ui";export default async function Page({searchParams}:{searchParams:Promise<{status?:string}>}){const {status}=await searchParams;const ok=status==="success";return <PageLayout><PageHero title={ok?"پرداخت موفق":"پرداخت ناموفق"} description={ok?"تراکنش آزمایشی تأیید شد.":"پرداخت تکمیل نشد."}/><section className="py-16"><Container><Card className="p-8 text-center"><p>{ok?"سفارش شما وارد مرحله پردازش شد.":"می‌توانید دوباره از صفحه سفارش اقدام کنید."}</p><Link className="btn btn-primary mt-5" href="/orders">مشاهده سفارش‌ها</Link></Card></Container></section></PageLayout>}
