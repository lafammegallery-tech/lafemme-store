import { requireAdmin } from "@/backend/auth/session";import { getPrisma } from "@/backend/database/prisma";import { toggleProductAction } from "@/app/actions/admin";import { PageLayout,PageHero } from "@/components/common";import Link from "next/link";import { Container,Card,Button,PriceDisplay } from "@/components/ui";
export const dynamic="force-dynamic";export default async function Page(){await requireAdmin();const items=await getPrisma().product.findMany({where:{deletedAt:null},include:{category:true,inventory:true},orderBy:{updatedAt:"desc"}});return <PageLayout><PageHero title="مدیریت محصولات" description="وضعیت، قیمت و موجودی محصولات"/><section className="py-12"><Container><div className="py-6"><Link href="/admin/products/new" className="btn btn-primary">افزودن محصول جدید</Link></div><div className="grid gap-4">{items.map(x=><Card key={x.id} className="p-5"><div className="flex flex-wrap justify-between gap-4"><div><h2>{x.name}</h2><p>{x.category.name} • {x.metalType} • {x.weight||"—"}</p><p>وضعیت: {x.status} | موجودی: {x.inventory?.quantity??x.stock}</p></div><div><PriceDisplay value={Number(x.price)}/><form action={toggleProductAction} className="mt-3"><input type="hidden" name="id" value={x.id}/><Button type="submit" variant="secondary">{x.status==="ACTIVE"?"غیرفعال‌کردن":"فعال‌کردن"}</Button></form></div></div></Card>)}</div></Container></section></PageLayout>}
