import {requireAdmin} from "@/backend/auth/session";import {getPrisma} from "@/backend/database/prisma";import {createProductAction} from "@/app/actions/admin";import {PageLayout,PageHero} from "@/components/common";import {Container,Input,Textarea,Button} from "@/components/ui";export const dynamic="force-dynamic";export default async function Page(){await requireAdmin();const cats=await getPrisma().category.findMany({where:{isActive:true,deletedAt:null}});return <PageLayout><PageHero title="محصول جدید" description="ثبت محصول و موجودی اولیه"/><Container><form action={createProductAction} className="grid gap-4 max-w-2xl py-10"><Input name="name" placeholder="نام محصول" required/><Input name="slug" placeholder="slug-english" dir="ltr" required/><Input name="sku" placeholder="SKU" dir="ltr"/><select name="categoryId" required className="border rounded-xl p-3">{cats.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select><select name="metalType" className="border rounded-xl p-3"><option value="GOLD">طلا</option><option value="SILVER">نقره</option><option value="PLATINUM">پلاتین</option></select><Input name="weight" placeholder="وزن نمایشی، مثال 5 گرم"/><Input name="weightValue" type="number" step="0.001" placeholder="وزن عددی"/><Input name="purity" placeholder="عیار"/><Input name="price" type="number" placeholder="قیمت پایه (تومان)" required/><Input name="stock" type="number" placeholder="موجودی" required/><Textarea name="shortDescription" placeholder="توضیح کوتاه"/><Button type="submit">ثبت محصول</Button></form></Container></PageLayout>}