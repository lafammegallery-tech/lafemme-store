import { requireAdmin } from "@/backend/auth/session";import { getPrisma } from "@/backend/database/prisma";import { PageLayout,PageHero } from "@/components/common";import { Container,Card } from "@/components/ui";export const dynamic="force-dynamic";export default async function Page(){await requireAdmin();const users=await getPrisma().user.findMany({where:{deletedAt:null},include:{_count:{select:{orders:true,addresses:true}}},orderBy:{createdAt:"desc"},take:100});return <PageLayout><PageHero title="کاربران" description="نمایش حساب‌ها و فعالیت خرید"/><section className="py-12"><Container><div className="grid gap-3">{users.map(u=><Card key={u.id} className="p-5"><h2>{[u.firstName,u.lastName].filter(Boolean).join(" ")||"کاربر"}</h2><p>{u.phone} • {u.email||"بدون ایمیل"}</p><p>نقش: {u.role} | سفارش‌ها: {u._count.orders} | آدرس‌ها: {u._count.addresses}</p></Card>)}</div></Container></section></PageLayout>}
