import { requireAdmin } from "@/backend/auth/session"; import { getPrisma } from "@/backend/database/prisma"; import { PageLayout, PageHero } from "@/components/common"; import { Container, Card, PriceDisplay } from "@/components/ui"; import Link from "next/link";
export const dynamic="force-dynamic";
export default async function AdminPage(){await requireAdmin();const p=getPrisma();const [users,orders,products,revenue]=await Promise.all([p.user.count({where:{deletedAt:null}}),p.order.count({where:{deletedAt:null}}),p.product.count({where:{deletedAt:null}}),p.order.aggregate({_sum:{totalAmount:true},where:{status:{in:["CONFIRMED","PROCESSING","SHIPPED","DELIVERED"]}}})]);return <PageLayout><PageHero title="مدیریت La Femme" description="نمای کلی فروشگاه"/><section className="py-16"><Container><div className="dashboard-stats"><Card className="stat-card"><h3>کاربران</h3><span>{users.toLocaleString("fa-IR")}</span></Card><Card className="stat-card"><h3>سفارش‌ها</h3><span>{orders.toLocaleString("fa-IR")}</span></Card><Card className="stat-card"><h3>محصولات</h3><span>{products.toLocaleString("fa-IR")}</span></Card><Card className="stat-card"><h3>فروش ثبت‌شده</h3><PriceDisplay value={Number(revenue._sum.totalAmount||0)}/></Card></div><Card className="p-6 mt-8"><h2>عملیات مدیریتی</h2><div className="mt-4 flex flex-wrap gap-3"><Link className="btn btn-primary" href="/admin/products">محصولات</Link><Link className="btn btn-primary" href="/admin/orders">سفارش‌ها</Link><Link className="btn btn-primary" href="/admin/inventory">موجودی</Link><Link className="btn btn-secondary" href="/admin/users">کاربران</Link><Link className="btn btn-secondary" href="/admin/market-prices">قیمت بازار</Link></div></Card></Container></section></PageLayout>}
