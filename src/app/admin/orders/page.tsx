import Link from "next/link";import { requireAdmin } from "@/backend/auth/session";import { getPrisma } from "@/backend/database/prisma";import { PageLayout,PageHero } from "@/components/common";import { Container,Card,PriceDisplay } from "@/components/ui";export const dynamic="force-dynamic";export default async function Page(){await requireAdmin();const rows=await getPrisma().order.findMany({where:{deletedAt:null},include:{payments:true,shipment:true},orderBy:{createdAt:"desc"},take:100});return <PageLayout><PageHero title="مدیریت سفارش‌ها" description="پیگیری پرداخت، پردازش و ارسال"/><section className="py-12"><Container><div className="grid gap-4">{rows.map(o=><Link key={o.id} href={`/admin/orders/${o.id}`}><Card className="p-5"><div className="flex flex-wrap justify-between gap-3"><div><h2>{o.orderNumber}</h2><p>{o.customerName} • {o.customerPhone}</p><p>{o.status} • پرداخت: {o.payments[0]?.status??"—"} • ارسال: {o.shipment?.status??"—"}</p></div><PriceDisplay value={Number(o.totalAmount)}/></div></Card></Link>)}</div></Container></section></PageLayout>}
